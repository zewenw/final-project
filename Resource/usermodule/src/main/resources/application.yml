server:
  port: 10001

spring:
  profiles:
    active: local
  application:
    name: usermodule

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true
info:
  app:
    name: usermodule
    description: usermodule server component
    version: 0.0.1-SNAPSHOT

custom:
  id: 4