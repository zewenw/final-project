server:
  port: 9000

spring:
  application:
    name: authenrization-client
  profiles:
    active: '@environment@'
  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: *