apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authserver.fullname" . }}-test-connection"
  labels:
    {{- include "authserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
