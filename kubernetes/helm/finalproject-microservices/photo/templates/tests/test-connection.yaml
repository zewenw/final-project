apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "photo.fullname" . }}-test-connection"
  labels:
    {{- include "photo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "photo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
