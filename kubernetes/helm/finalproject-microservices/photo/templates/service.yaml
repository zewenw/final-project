apiVersion: v1
kind: Service
metadata:
  name: {{ include "photo.fullname" . }}
  labels:
    {{- include "photo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "photo.selectorLabels" . | nindent 4 }}
